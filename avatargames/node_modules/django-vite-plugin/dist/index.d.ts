import { Plugin } from 'vite';
interface PluginConfig {
    /**
     * The path or paths of the entry points to compile.
     */
    input: string | string[];
    /**
     * The root path of the project relative to the `vite.config.js` file
     */
    root?: string;
    /**
     * If the aliases should be added in the `jsconfig.json` or not
     */
    addAliases?: boolean;
    /**
     * Path to python executable
     */
    pyPath?: string;
    /**
     * Path to python executable
     */
    pyArgs?: Array<string>;
}
export default function djangoVitePlugin(config: PluginConfig | string | string[]): Promise<Plugin[]>;
export {};
